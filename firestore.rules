rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /item_seen/{itemId}/viewers/{sellerId} {
      allow read, write: if true;
    }

    match /{document=**} {
      allow read, write: if true;
    }
  }
}
